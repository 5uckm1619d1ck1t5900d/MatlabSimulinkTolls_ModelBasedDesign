%  CreateLCTFiles creates necessary interface files for a given FDD's
%  DataDictionary.
%  function cals = CreateLCTFiles(datadicts,srcfile)
%  Required Parameters
%    datadicts -  Cell array with the names of data dictionaries that are going
%                to be used to create CalConstants.h file needed for the
%                LCT. Strings in the cell array can have the path to the
%                desired file.
% 
%
