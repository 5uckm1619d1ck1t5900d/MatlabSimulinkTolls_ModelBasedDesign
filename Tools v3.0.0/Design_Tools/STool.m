%  STOOL MATLAB code for STool.fig
%       STOOL, by itself, creates a new STOOL or raises the existing
%       singleton*.
% 
%       H = STOOL returns the handle to a new STOOL or the handle to
%       the existing singleton*.
% 
%       STOOL('CALLBACK',hObject,eventData,handles,...) calls the local
%       function named CALLBACK in STOOL.M with the given input arguments.
% 
%       STOOL('Property','Value',...) creates a new STOOL or raises the
%       existing singleton*.  Starting from the left, property value pairs are
%       applied to the GUI before STool_OpeningFcn gets called.  An
%       unrecognized property name or invalid value makes property application
%       stop.  All inputs are passed to STool_OpeningFcn via varargin.
% 
%       *See GUI Options on GUIDE's Tools menu.  Choose "GUI allows only one
%       instance to run (singleton)".
% 
%  See also: GUIDE, GUIDATA, GUIHANDLES
%
